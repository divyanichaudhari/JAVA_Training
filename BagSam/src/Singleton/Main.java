package Singleton;

import Singleton.Singleton;
public class Main {
	public static void main(String[] args) {
        Singleton bg = Singleton.display();
        
        Singleton bg1 = Singleton.display();
    }
}
